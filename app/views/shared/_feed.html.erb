<% if @feed_items.any? %>
	<ol class="microposts">
		<!-- passing collection @feed_items lets us render partial with feed items -->
		<!-- partial must exist so that we can pass collection -->
		<%= render partial: 'shared/feed_item', collection: @feed_items %>
	</ol>
	<%= will_paginate @feed_items %>
<% end %>